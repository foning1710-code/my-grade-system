services:
  - type: web
    name: school-report-system
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGODB_URI
        value: "your-mongodb-connection-string-here" # We'll get this in next step